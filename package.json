{
  "name": "telemicroscope",
  "version": "0.0.0",
  "description": "Physics-based telescope and microscope simulator on a shared, validated optical engine",
  "author": "Boyko Neov",
  "license": "SEE LICENSE IN LICENSE",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ports": "node scripts/port-guard.test.mjs",
    "typecheck": "tsc --noEmit -p packages/core && tsc --noEmit -p packages/core/tsconfig.test.json && tsc --noEmit -p packages/app",
    "dev": "npm run dev --workspace @telemicroscope/app"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/react": "^18.3.31",
    "@types/react-dom": "^18.3.7",
    "@vitejs/plugin-react": "^4.7.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
